spring.application.name=dem_step_websocket_v01
# 0. UTF-8 설정
# 웹 서버의 인코딩을 UTF-8로 강제합니다.
# 한글이 깨지는 현상을 방지하기 위해 필수적이며,
# 모든 HTTP 요청과 응답의 문자 인코딩을 UTF-8로 통일시켜 줍니다.
server.servlet.encoding.charset=utf-8
server.servlet.encoding.force=true


# 1. DB 연결
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:test
spring.datasource.username=sa

# 2. hibernate 세팅 (조건 : @Entity가 붙어있는 클래스가 찾아서 생성한다)
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.format_sql=true

# 스프링 부트의 spring-boot-starter-mustache 의존성을 추가하면 기본 속성으로 설정 됨
# 템플릿 파일 위치: src/main/resources/templates/
# 파일 확장자: .mustache